# IRON MAN 점검 결과 (CHECK)

> 점검일: 2026-09-18 (배포 점검 뒤 최종) · 기준 문서: [PLAN.md](PLAN.md) 성공 기준, [DESIGN.md](DESIGN.md) v0.3 · 점검 도구: bkit gap-detector, bkit security-architect
> 배포 주소: https://iron-man-neon.vercel.app · 공개 코드 저장소: https://github.com/yseo217/iron-man (데이터·알림 발송은 비공개 저장소 yseo217/my-app)

## 판정: ✅ 통과 (성공 기준 11/11, 배포 점검 5/5 — 발표 준비 완료)

| 점검 | 결과 | 요약 |
|---|---|---|
| 성공 기준 (PLAN.md) | ✅ **11 / 11 충족** | 마지막 남았던 "휴대폰 홈 화면에 추가"를 배포 뒤 아이폰에서 확인 |
| 설계 일치 (Gap) | ✅ 1차 **약 89%** → 16개 중 **15개 해결**, 1개 남음 (선택) | 남은 것: PIN 잠금 장난(Gap 10). 해결 뒤 일치율은 다시 재지 않음 |
| 보안 점검 | ✅ 치명적 위험 없음 | 코드로 고칠 8개 고침, PIN 변경, 데이터 저장소 비공개 확인, 배포 서버 토큰을 데이터 저장소 전용으로 교체, `npm audit` 취약점 0개. 남은 설정 2개는 선택 |
| 배포 점검 (Part 5) | ✅ **5 / 5 통과** | 저장소 기록 모두 올라감, 공개 저장소에 README 있고 `.env`·`data/` 없음, Vercel 연결, 배포 주소 200, 환경 변수는 앱이 쓰는 키 7개만 |

---

## 고쳐야 할 항목 (우선순위 순) — 필수 없음, 선택 3개

필수 항목은 모두 끝났어요. 아래는 사고가 났을 때 피해를 줄이는 보험이에요 (사용자 판단 2026-09-18).

| # | 항목 | 출처 | 안 하면 생기는 일 | 누가 |
|---|---|---|---|---|
| 1 | OpenAI 월 사용 한도 걸기 (**2분이면 돼서 추천**) | 보안 7 | 누가 옛 기사 화면을 대량으로 열면 요약이 몰려 한 번에 수십 달러 정도 나올 수 있어요 | 사용자 |
| 2 | Vercel 방화벽으로 접속 횟수 제한 | 보안 3·4 | GitHub 읽기 60초 재사용과 모델 버튼 PIN으로 위험이 많이 줄었어요. 남은 건 PIN 잠금 장난 정도예요 | Claude (문제가 생기면) |
| 3 | 누구든 PIN을 5번 틀리면 나까지 10분 잠김 | Gap 10 · 보안 4 | PIN을 영문+숫자로 바꿔 맞히기는 어려워졌어요. 실제로 잠기는 일이 생기면 2번을 하거나 틀린 횟수를 저장소에 기록해요 | Claude (필요할 때) |

> 관리 메모: 공개 저장소(iron-man)는 비공개 저장소(my-app)의 코드 사본이에요 (`data/`, `.github` 제외). 코드를 고치면 공개 저장소에도 새 커밋으로 맞춰 올리고, 배포는 Vercel CLI로 해요 (`vercel.json`으로 GitHub 자동 배포는 꺼 둠).

---

## 배포 점검 (Part 5)

| # | 항목 | 결과 | 근거 |
|---|---|---|---|
| 1 | origin이 GitHub 주소, 안 올린 커밋 없음 | ✅ | `https://github.com/yseo217/my-app.git`, 안 올린 커밋 0개, 커밋 안 한 수정 0개 |
| 2 | 공개 저장소, README 있음, `.env` 없음 | ✅ | [yseo217/iron-man](https://github.com/yseo217/iron-man) 공개, README.md 있음, `.env`·`data/` 없음 (로그인 없이 확인). 데이터 저장소 my-app은 비공개(로그인 없이 404) |
| 3 | Vercel 프로젝트 연결 (`.vercel`) | ✅ | 프로젝트 `iron-man` |
| 4 | 배포 URL 200, 앱 제목 보임 | ✅ | https://iron-man-neon.vercel.app → HTTP 200, `IRON MAN`, "AI와 반도체 주요 기사" |
| 5 | 앱이 쓰는 키만 등록 | ✅ | `DATA_REPO_TOKEN`, `GITHUB_REPO`, `SUBSCRIBE_PIN`, `OPENAI_API_KEY`, 알림 키 3개. `GITHUB_TOKEN`·`VERCEL_TOKEN`·`SUPABASE_ACCESS_TOKEN` 없음 |

결론: **발표 준비 완료**

---

## 성공 기준 판정 (PLAN.md)

| # | 성공 기준 | 판정 | 직접 확인하는 방법 |
|---|---|---|---|
| 1 | lint·build 오류 없음 | ✅ | `npm run lint`, `npm run build` 실행 |
| 2 | 메인 화면에 기사 5건, 없으면 "지금 새 기사가 없어요" | ✅ | `npm run dev` → 카드 5장 확인. 빈 화면은 와이파이를 끄고 새로고침 |
| 3 | 휴대폰 홈 화면에 앱으로 추가 | ✅ | 2026-09-18 배포 주소에서 아이폰으로 알림을 켬 (아이폰은 홈 화면에 추가해야만 알림을 켤 수 있음, 저장된 알림 서버 web.push.apple.com). 직접 확인: 휴대폰으로 배포 주소 → "홈 화면에 추가" |
| 4 | Actions 직접 실행 → 알림 5건, 누르면 기사 화면 | ✅ | GitHub → Actions → notify → Run workflow |
| 5 | 틀린 PIN으로는 알림을 켜거나 끌 수 없음 | ✅ | 틀린 PIN으로 "알림 켜기" → "PIN이 맞지 않아요" |
| 6 | 다시 실행하면 보낸 기사는 빠지고 다음 순위 기사 | ✅ | 발송 뒤 `npm run notify -- --dry-run` 결과가 다른지 확인 |
| 7 | 발송 실패 시 1번 다시 시도하고 기록을 남김 | ✅ | `data/sent-articles.json`의 `failures` 칸 확인 (실제로 실패했을 때만 보임) |
| 8 | (Nice) 한국어 제목·3줄 요약·쉬운 설명 | ✅ | 메인·지난 기사 화면에 ①②③ 3줄이 보이는지 확인 (저장된 요약 16개 모두 3줄) |
| 9 | (Nice) 모델 버튼으로 고르면 저장, 새 요약부터 적용 | ✅ | PIN을 넣고 다른 모델을 누른 뒤 새로고침 |
| 10 | (Nice) 지난 기사 목록을 최신순으로 | ✅ | 메뉴 "지난 기사"에서 맨 위가 가장 최근 회차인지 확인 |
| 11 | (Nice) 메인 화면에 주요 뉴스 영상 1개 | ✅ | "오늘의 주요 영상" 재생 (유튜브 RSS가 불안정해 가끔 숨겨질 수 있음) |

운영 지표(알림 성공 95%, 오픈율 80%, 검색 시간 주 1시간 이하)는 며칠 써 본 뒤 판단해요.

---

## 해결된 항목 (참고)

**Gap (설계 ↔ 코드)**
- Gap 1 — 알림 상태를 서버와 맞춰 확인해요. 서버에 없으면 "다시 켜 주세요" 안내가 나와요.
- Gap 2 — 인터넷이 끊기면 안내 문구를 보여주고 이 기기의 구독도 되돌려요. 예상 못 한 오류에는 전용 오류 화면을 보여줘요.
- Gap 3 — 동시에 저장해 충돌하면 최신 내용을 다시 읽어 합쳐 저장해요.
- Gap 4 — 저장소에 "요약 중" 표시를 남겨서, 서버가 여러 대여도 한 번만 요약해요.
- Gap 5 — 구글 뉴스 검색이 모두 실패하면 발송 작업도 실패로 끝나요.
- Gap 11 — 모델 버튼에도 PIN을 받아요.
- Gap 12 — 설계서를 "GitHub에서 읽은 내용은 60초 동안 다시 씀"으로 고쳤어요.
- Gap 15·16 — 설계서를 v0.3으로 최신화했어요.
- Gap 6 — 만료된 기기 정보를 못 지워도 보낸 기사 기록은 저장하고, 발송은 실패로 끝내요.
- Gap 7 — 저장소의 기기가 방금 보낸 기기와 같을 때만 지워서, 그 사이 새로 켠 알림은 남겨요.
- Gap 8 — 보낸 기사 파일 모양이 깨졌으면 빈 기록으로 덮어쓰지 않고 멈춰요.
- Gap 9 — 알림을 누르면 열린 창에서 기사 화면으로 이동하고, 이동이 안 되면 새 창으로 열어요.
- Gap 14 — Vercel에 배포했고, GitHub 기록마다 자동 배포되지 않게 껐어요 (`data/` 저장으로 다시 배포되는 문제 해결).
- Gap 13 — 영상이 없으면 "지금은 보여줄 영상이 없어요" 작은 칸을 보여줘서 메뉴 "영상" 링크가 갈 곳이 있어요.

**보안 (코드로 고친 것)**
- 모델 버튼 PIN (보안 1)
- GitHub 읽기 60초 재사용, 다른 서버 요약 기다림 90초 → 30초 (보안 3)
- PIN 입력칸을 일반 키보드로 (보안 4)
- 보안 헤더: 허락한 곳의 스크립트만 실행(CSP), HTTPS만 사용(HSTS), 카메라·마이크·위치 차단, `X-Powered-By` 숨김 (보안 8)
- 요약에서 http 없는 주소 글자도 지우기 (보안 9, 프롬프트 인젝션 대비)
- 알림 주소는 브라우저 회사 알림 서버만 허용 (보안 10)
- 검색엔진 등록 차단 (보안 11)
- PIN을 6자 이상 영문+숫자로 변경 (보안 4, 사용자가 `.env`에서 직접 바꿈)
- GitHub 토큰 권한 줄이기 (보안 6): 배포 서버는 my-app 파일 읽기·쓰기만 되는 세분화 토큰 `DATA_REPO_TOKEN`을 쓰고, 관리용 `GITHUB_TOKEN`은 Vercel에서 뺌. 다른 저장소 쓰기는 거절(403) 확인 (2026-09-18)
- 저장소 비공개 확인 (보안 5): 로그인 없이 저장소 주소를 열면 404 (2026-09-18)
- 발송 작업의 GitHub 도구(checkout, setup-node)를 정확한 버전 코드로 고정, `npm audit` 취약점 0개 (보안 12)

**이미 잘 되어 있던 것 (보안)**
- 비밀 키는 `.env`에만 있고 GitHub 기록에 올라간 적이 없어요. 브라우저에는 공개용 알림 키만 가요.
- 모든 입력(PIN, 알림 정보, 모델, 기사 번호)을 검사한 뒤에 써요. PIN은 응답 시간으로 추측할 수 없게 비교해요.
- 기사·AI 요약은 글자로만 표시돼서 스크립트가 실행되지 않아요. 알림을 누르면 이 앱 안의 화면만 열려요.
- 오류가 나도 속 내용은 화면에 보이지 않아요.
- 개인정보는 받지 않아요. 저장하는 건 알림 받을 기기 정보 하나뿐이에요.

## 검증 기록

- 자동 시험 12개 통과: 저장 충돌, 요약 중 표시, 주소 지우기, 알림 주소 제한, 60초 재사용, 만료 기기 정리, 깨진 기록 보호 (가짜 GitHub·OpenAI 사용)
- 발송 스크립트 미리보기(`npm run notify -- --dry-run`) 정상: 새 기사 5건 선택
- `npm run lint`, 타입 검사, `npm run build` 통과
- 브라우저(배포용 빌드): 카드 5장, 3줄 요약, 영상, 알림 칸 정상. 보안 헤더 때문에 막힌 곳 없음
- 배포(Vercel CLI): 환경 변수 7개 등록(관리용 VERCEL_TOKEN·SUPABASE_ACCESS_TOKEN 제외). 배포 주소에서 카드 5장, 3줄 요약, 영상, 알림 칸, 모델 선택, 지난 기사 15건, 기사 화면, 보안 헤더, 앱 정보 파일, 서비스 워커 정상, 콘솔 오류 없음
- 공개 저장소: 로그인 없이 봐도 `data/` 폴더 없음(404), `.env`·`node_modules` 없음
- 배포 서버 토큰 교체: 데이터 저장소 전용 토큰은 my-app 쓰기 가능, 다른 저장소 쓰기 거절(403). 교체 뒤 배포 주소에서 지난 기사 15건·기사 화면 정상
- 배포 주소에서 아이폰 알림 켜기 확인 (2026-09-18 05:01 UTC)
- 모델 버튼: PIN 없이 누르거나 틀린 PIN을 넣으면 거절되는 것 확인 (맞는 PIN은 사용자가 직접 확인)

# IRON MAN 개발 계획 (PLAN)

> 기준 문서: [PRD.md](PRD.md) · 작성자: Tony Stark · 작성일: 2026-09-17 · 상태: 초안

## 이번 사이클 목표

앱을 열면 지금 주목받는 AI·반도체 해외 기사 5건을 보여주고, 매일 오전 8시·오후 8시(한국시간)에 그 5건을 휴대폰 알림으로 보내는 개인용 웹앱을 완성한다. 여유가 있으면 한국어 요약·쉬운 설명, 요약 모델 고르기, 지난 기사 목록까지 만든다.

## 성공 기준

- [x] `npm run lint`, `npm run build`가 오류 없이 통과한다
- [x] 메인 화면에 AI·반도체 기사 5건이 보이고, 기사가 없으면 "지금 새 기사가 없어요"가 보인다 — 빈 상태는 인터넷 끊김 시험(기사 0건)으로 확인
- [ ] 휴대폰 홈 화면에 앱으로 추가할 수 있다 — 앱 정보·아이콘은 확인 완료, 휴대폰 설치는 Part 5 배포 후 (23번)
- [x] GitHub Actions를 직접 실행하면 알림 5건(새 기사가 적으면 그만큼)이 도착하고, 알림을 누르면 그 기사 화면이 열린다 — 2026-09-17 실행 35191666644: 발송 5·실패 0, 기사 화면 열림 확인 (알림 클릭 자체는 사용자 크롬에서 확인)
- [x] 틀린 PIN으로는 알림을 켜거나 끌 수 없다 — 단위 시험 (틀림·5번 틀리면 10분 잠금·PIN 미설정 거절)
- [x] 한 번 더 실행하면 이미 보낸 기사는 빠지고 다음 순위 기사가 온다 — 발송 후 미리보기 실행에서 새 5건 선택 확인
- [x] 발송이 실패하면 1번 다시 시도하고 실패 기록이 남는다 — 단위 시험 (재시도·만료 시 재시도 안 함·실패 기록 저장)
- [x] (Nice) 기사마다 한국어 제목·3줄 요약(2026-09-18 1~2줄에서 변경)·쉬운 설명이 붙는다 — 화면·알림 모두 확인
- [x] (Nice) 요약 모델 버튼으로 PIN을 넣고(2026-09-18 추가) GPT-5.5 / GPT-5.4 mini를 고르면 저장되고 새 요약부터 적용된다 — 화면에서 바꾸고 새로고침 후 유지 확인, GPT-5.4 mini 실제 요약 확인 (2026-09-17)
- [x] (Nice) 지난 기사 목록 화면에서 보낸 기사를 최신순으로 볼 수 있다
- [x] (Nice, 10-1번) 메인 화면에 주요 뉴스 영상 1개가 나온다 — 유튜브 RSS가 불안정해 영상이 빠지는 때가 있음
- 운영 지표(PRD 3번: 30분 이내 알림 성공 95%, 오픈율 80%, 검색 시간 주 1시간 이하)는 며칠 써본 뒤 GitHub Actions 실행 기록과 본인 체감으로 판단한다 (별도 측정 기능은 만들지 않음)

## 한눈에 보기 (Executive Summary)

| 관점 | 내용 |
|------|------|
| **문제** | 비전문가라 쏟아지는 해외 AI·반도체 기사 중 무엇을 읽을지 고르기 어렵고, 직접 찾는 데 시간이 든다 |
| **해결** | 구글 뉴스 RSS의 주요 기사 5건을 앱 화면과 하루 2번 알림으로 전달한다 |
| **사용 효과** | 앱을 열거나 알림을 누르기만 하면 지금 중요한 기사를 바로 본다 |
| **핵심 가치** | 찾아다니지 않아도 관심 분야 흐름을 놓치지 않는다 |

## Context Anchor

| 항목 | 내용 |
|------|------|
| **WHY** | 비전문가가 중요한 AI·반도체 기사를 스스로 고르기 어렵다 |
| **WHO** | Tony Stark 1명 (로그인 없는 개인용) |
| **RISK** | GitHub Actions 예약 실행 지연, 아이폰은 홈 화면에 추가해야 알림 수신, 기기 정보가 저장소에 들어가므로 저장소 공개 금지 |
| **SUCCESS** | 화면에 기사 5건 표시, 하루 2회 알림 5건 도착, 중복 기사 제외 |
| **SCOPE** | 1단계 기반 준비 → 2단계 실시간 화면 → 3단계 앱 설정·알림 → 4단계 Nice 기능 → 5단계 최종 확인 |

## 정해진 방식

| 항목 | 결정 |
|------|------|
| 기사 출처·인기 순서 | 구글 뉴스 RSS (API 키 없음, 구글 뉴스의 주요 기사 순서를 인기 순서로 사용). AI 검색 3건 + 반도체 검색(최근 1일) 2건 |
| 저장 위치 | 비공개 GitHub 저장소 안의 파일 (`data/sent-articles.json`, `data/subscription.json`, Nice: `data/summaries.json`) |
| 예약 실행 | GitHub Actions — UTC 23:00, 11:00 (= 한국시간 08:00, 20:00) |
| 알림 방식 | 웹 푸시, 한 번 실행에 기사별 알림 5건 |
| 한국어 요약 (Nice) | OpenAI API (`.env`의 `OPENAI_API_KEY`) |
| 기술 스택 | Next.js 고정, 배포는 Vercel |

## 작업 순서

### 1단계 — 기반 준비
1. 비공개 GitHub 저장소를 만들고 `my-app`을 올린다 (`.env`가 빠졌는지 먼저 확인)
2. 설치된 Next.js 16 문서(`node_modules/next/dist/docs/`)에서 화면·API 경로·앱 설정 방법을 확인한다
3. 기사 한 건의 모양(제목, 링크, 매체, 발행 시각, 고유 번호)을 정한다
4. 저장 파일 두 개의 모양(보낸 기사 목록, 알림 받을 기기 정보)을 정하고 빈 파일을 만든다

### 2단계 — 실시간 인기 기사
5. 구글 뉴스 RSS에서 AI 기사와 반도체 기사를 따로 검색해 읽어오는 기능을 만든다 (XML 읽기 라이브러리는 설치 전에 확인받기)
6. 가져온 기사에서 AI 3건 + 반도체 2건을 고르고, 필요하면 이미 보낸 기사를 빼는 기능을 만든다
7. 화면이 서버에서 인기 기사 5건을 바로 가져오게 하고, 10분 동안 결과를 재사용하게 한다
8. 메인 화면에 기사 5건을 보여주고, 없으면 "지금 새 기사가 없어요"를 띄운다
9. 알림을 눌렀을 때 열릴 기사 화면을 만든다

### 3단계 — 앱 설정과 알림
10. 홈 화면에 추가할 수 있게 앱 정보(이름, 아이콘)를 설정한다
10-1. (Nice, 사용자 요청으로 먼저 진행) 뉴스 채널 유튜브 RSS에서 최근 2일 AI·반도체 영상 중 조회수 1위를 골라 메인 화면에 재생 칸으로 넣는다 (채널별 12시간 저장, 실패 시 "영상 없음" 작은 칸)
11. 알림을 받아 띄우고, 누르면 기사 화면으로 여는 서비스 워커를 만든다
12. 웹 푸시용 키를 만들어 `.env`에 넣는다 (값은 채팅에 출력하지 않음)
13. PIN을 확인하는 "알림 켜기·끄기" 버튼과, 기기 정보를 저장소 파일에 기록하는 서버 동작을 만든다
14. 기사 최대 5건을 알림으로 보내고, 실패하면 1번 다시 시도한 뒤 기록을 남기는 발송 스크립트를 만든다 (기기 정보 만료 시에는 재시도 없이 기록)
15. 발송이 끝나면 보낸 기사 파일을 갱신해 저장소에 기록하게 한다 (최근 90일치만 보관)
16. 하루 2번 발송 스크립트를 실행하는 GitHub Actions 설정을 만들고, 필요한 키를 저장소 Secrets에 등록한다
17. Actions를 직접 한 번 실행해 알림 도착, 누르면 기사 화면 열림, 두 번째 실행 때 중복 제외까지 확인한다

### 4단계 — Nice to have
18. OpenAI로 한국어 제목·3줄 요약·새 기술 쉬운 설명을 만드는 기능을 만들고, 같은 기사를 다시 요약하지 않게 결과를 `data/summaries.json`에 저장한다
19. 메인 화면, 기사 화면, 알림에 한국어 요약을 붙인다
20. ~~요약에서 어려운 용어를 골라 구글 사전 검색 링크를 붙인다~~ → (2026-09-17 사용자 요청으로 변경) 요약 AI 모델(GPT-5.5 / GPT-5.4 mini)을 고르는 버튼을 만들고, 고른 모델을 `data/settings.json`에 저장해 새 요약부터 쓴다
21. 최근 90일 동안 보낸 기사를 최신순으로 보여주는 지난 기사 목록 화면을 만든다

### 5단계 — 마무리 확인
22. ✅ (2026-09-17 완료) 검증 루프(`npm run lint` → `npm run build` → `npm run dev`로 화면 확인)를 돌리고, 성공 기준을 하나씩 체크한다 (알림 확인은 HTTPS 모드인 `npx next dev --experimental-https`로 실행)
23. 휴대폰에서 알림을 받으려면 인터넷 주소(HTTPS)가 필요해서, Part 5에서 Vercel 환경변수를 등록하고 배포한 뒤 휴대폰으로 확인한다

## 위험 요소와 대응

| 위험 | 대응 |
|------|------|
| GitHub Actions 예약 실행이 수 분~수십 분 늦을 수 있음 | 성공 기준에 30분 이내 지연을 허용하고, 실행 기록으로 누락 여부를 확인 |
| 구글 뉴스 RSS 형식이 바뀌거나 막힐 수 있음 | 읽기 실패 시 "새 기사 없음"으로 처리하고 실패 기록 남김 |
| 유튜브 채널 RSS가 자주 404·500으로 실패함 (2026-09-17 시험: 6곳 중 1~2곳만 성공) | 여러 채널을 함께 보고, 성공한 채널 결과만 12시간 저장. 모두 실패하면 "영상 없음" 작은 칸. 계속 불안정하면 YouTube Data API(키 필요)로 전환 검토 |
| 아이폰은 홈 화면에 추가해야 알림을 받음 | 알림 켜기 버튼 옆에 안내 문구 표시 |
| 기기 정보가 저장소 파일에 들어감 | 저장소를 비공개로 만들고, 공개 전환 금지를 규칙으로 유지 |
| 서버(Vercel)는 파일을 직접 저장할 수 없음 | 기기 정보 저장은 GitHub API와 `GITHUB_TOKEN`으로 저장소에 기록 |
| 앱을 열 때마다 요약하면 OpenAI 비용이 늘어남 (사용자 선택: gpt-5.5 + 웹 검색으로 기사당 비용이 큰 편) | 요약 결과를 저장해 같은 기사는 다시 요약하지 않음. 비용이 크면 메인 화면의 요약 모델 버튼에서 GPT-5.4 mini로 바꿈 |

## 다음 단계

- 설계는 [DESIGN.md](DESIGN.md)에 정리됨. 다음은 구현 단계
- 저장소 파일이 바뀔 때마다 Vercel이 다시 배포하지 않도록, Part 5에서 `data/`만 바뀐 커밋은 배포를 건너뛰게 설정한다

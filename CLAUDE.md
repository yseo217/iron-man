@AGENTS.md

# IRON MAN 프로젝트 규칙서 (CLAUDE.md)

이 문서는 `my-app` 프로젝트에서 작업할 때 지켜야 할 규칙입니다. 자세한 기획 내용은 [PRD.md](PRD.md)를 참고하세요.

## 프로젝트 개요

구글 뉴스 RSS에서 AI 주요 기사 3건과 반도체 주요 기사 2건을 골라 보여주는 개인용 웹앱. 12시간마다 주요 기사 최대 5건을 앱 알림(웹 푸시)으로 보냄. 한국어 요약은 여유가 있으면 추가하는 기능(Nice to have). 사용자는 작성자 본인 1명뿐. 설계는 [PLAN.md](PLAN.md), [DESIGN.md](DESIGN.md) 참고.

## 언어

- 모든 설명과 코드 주석은 한국어로 작성한다.
- 설명은 어려운 용어 대신 쉬운 말로 한다.

## 기술 스택 (고정)

- PRD에 정한 대로 **Next.js**(TypeScript + Tailwind CSS + ESLint, App Router)로 고정한다.
- 다른 프레임워크로 바꾸거나 마이그레이션을 제안하지 않는다.
- 배포는 **Vercel**을 사용한다.
- 새로운 라이브러리나 서비스를 추가하기 전에는 먼저 사용자에게 물어보고 결정한다.

## 파일 규칙

- 새 파일은 `my-app` 폴더 안에만 만든다.
- 파일을 지워야 할 때는 바로 삭제하지 않는다. `my-app/trash-can/` 폴더를 만들어 그 안으로 옮겨만 둔다. 실제 삭제는 작업이 끝난 뒤 사용자가 직접 확인하고 한다.

## 비밀 정보 / 보안 규칙

- 모든 API 키·토큰은 `.env`에만 저장한다. 코드에 절대 직접 적지 않는다.
- `.env` 등 비밀 정보 파일과 `node_modules` 폴더는 `.gitignore`에 등록해 두고, 절대 커밋하지 않는다.
- 커밋하기 전에 비밀 값이 포함되지 않았는지 확인한다.
- 외부 서비스 인증이 필요하면 토큰 값을 사용자에게 묻거나 채팅에 출력하지 않는다. `.env`에 있는 값을 읽어서 사용한다.
  - 예: Supabase 작업이 필요하면 Supabase CLI를 설치하고 `.env`의 `SUPABASE_ACCESS_TOKEN`으로 작업한다.
  - 예: Vercel 작업(배포 등)이 필요하면 Vercel CLI를 설치하고 `.env`의 `VERCEL_TOKEN`으로 인증해 작업한다.
- 이 프로젝트는 회원가입/로그인 없는 개인용 비공개 서비스다. 여러 사용자를 지원하는 인증 기능은 추가하지 않는다.
- GitHub 저장소에 기록을 남기는 서버 동작(Server Action)은 누구나 부를 수 있으므로 반드시 PIN을 확인한다. AI가 만든 글은 링크·주소를 지우고 글자로만 보여준다. (2026-09-18 보안 점검에서 정함)

## 범위 규칙

- PRD.md의 "Must" 기능(현재 2개)을 우선 구현한다: ① 실시간 인기 기사 보기, ② 12시간마다 앱 알림.
- PRD.md "절대 만들지 않을 것"에 적힌 기능(다중 사용자, 관심 키워드 커스터마이징, 한국어 외 언어 출력 등)은 만들지 않는다.
- 범위를 벗어나는 새 기능이 필요해 보이면, 먼저 사용자에게 제안하고 PRD.md에 반영한 뒤에 구현한다.

## 작업 절차 (검증 루프)

코드를 바꿀 때마다 아래를 통과할 때까지 반복한다.

1. **변경한다**
2. **검사한다** — `npm run lint`와 `npm run build`를 실행해 오류가 없는지 확인한다.
3. **눈으로 확인한다** — 화면이 바뀐 작업이면 `npm run dev`로 띄워 실제로 보이는지 확인한다. 알림 관련 작업은 HTTPS가 필요하므로 `npx next dev --experimental-https`로 띄운다.
4. **문제가 있으면** 고치고 다시 1번으로 돌아간다.

모두 통과하면 무엇을 왜 바꿨는지 한 줄로 요약한다.

- 성공 기준·Gap·보안 판정은 문서의 체크 표시를 믿지 말고 지금 코드와 실제 실행으로 다시 확인한다. 결과는 `CHECK.md`를 새로 만들지 않고 덮어써 최신으로 유지한다. (2026-09-18)

## 협업 방식

- 코드를 바꾸면 반드시 **무엇을 왜 바꿨는지 한 줄로** 알려준다.
- 기획(PRD.md)을 바꿔야 할 만한 변경은 코드부터 짜지 말고, 먼저 어떻게 바꿀지 설명하고 사용자 확인을 받은 뒤 PRD.md와 코드를 함께 수정한다.
- 기능 단위(PRD.md의 "개발 단위")로 하나씩 만들고 확인받으며 진행한다.
- 이미 설치된 서브에이전트(bkit 에이전트 등)는 필요할 때마다 적극 활용한다.

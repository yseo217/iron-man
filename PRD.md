# IRON MAN 서비스 기획서 (PRD)

**작성자**: Tony Stark
**작성일**: 2026-09-17

> 용어: 이 문서에서 "주요 기사", "인기 기사"는 **구글 뉴스 RSS가 정한 순서의 상위 기사**를 뜻한다. 실제 조회수는 알 수 없다. 5건은 **AI 검색 상위 3건 + 반도체 검색(최근 1일) 상위 2건**으로 채운다.

## 1) 추진 배경 (문제 정의)

AI 기술과 반도체 시장에 관심이 많지만 관련 지식이 깊지 않은 사용자(작성자 본인)는, 해외 외신에서 쏟아지는 관련 기사 중 어떤 것이 중요하고 신뢰할 만한지 스스로 판단하기 어렵다. 그 결과 관심 분야의 최신 동향을 놓치거나, 정보를 얻기 위해 여러 매체를 직접 찾아다니며 시간을 들여야 하는 불편이 있다.

## 2) 현행 방식과 한계

현재는 특별한 도구 없이, 관심 있을 때마다 해외 IT·경제 매체를 직접 검색하거나 방문해 AI·반도체 관련 기사를 찾아 읽고 있다. 이 방식은 확인 주기가 불규칙하고, 어떤 기사가 실제로 많이 읽히고 화제가 되는지 알 방법이 없어 중요도가 낮은 기사에 시간을 쓰거나 중요한 소식을 놓치는 경우가 생긴다.

## 3) 목표와 기대효과 (성공 기준)

- 12시간 간격(하루 2회)으로 앱 알림이 예정 시각 30분 이내에, 누락 없이 발송되는 비율 95% 이상
- 알림으로 받은 기사 중 실제로 열어서 읽는 비율(오픈율) 80% 이상 유지 — 관심사에 맞는 기사가 선별되고 있다는 지표
- 기사를 직접 찾아보는 데 쓰는 시간을 주 1시간 이하로 단축

**측정 방법**: 며칠 사용해 본 뒤 GitHub Actions 실행 기록과 본인 체감으로 판단한다. 이를 위한 별도 측정 기능은 만들지 않는다.

## 4) 사용자와 이용 흐름

**사용자**: Tony Stark (AI·반도체에 관심 있는 개인 사용자, 비전문가)

**흐름 A (직접 볼 때)**: 앱 열기 → (Nice to have) 주요 뉴스 영상 재생 → 지금 AI·반도체 주요 기사 5건 확인 → (Nice to have) 한국어 요약·쉬운 설명 읽기 → 원문 링크 눌러 열람

**흐름 B (알림 받을 때)**: 처음 한 번 PIN 입력 후 알림 켜기 → 매일 오전 8시·오후 8시 → 앱 알림으로 주요 기사 최대 5건 도착 → 알림 누르기 → 앱의 기사 화면에서 확인 → 원문 열람

**흐름 C (다시 볼 때, Nice to have)**: 앱 열기 → 지난 기사 목록 화면 → 기사 누르기 → 기사 화면에서 요약·원문 확인

## 5) 주요 기능 (must / nice로 구분)

**Must (최대 2개)**
1. **실시간 인기 기사 보기**
   - AI가 지킬 규칙: 앱을 열거나 새로고침하면 별도 API 키 없이 구글 뉴스 RSS에서 AI 검색과 반도체 검색(최근 1일)을 따로 해서, 각 검색의 구글 뉴스 순서대로 AI 3건 + 반도체 2건을 표시 (한쪽이 모자라면 다른 쪽으로 채우고, 같은 기사는 한 번만) / 결과가 없으면 "지금 새 기사가 없어요" 안내
2. **12시간마다 앱 알림**
   - AI가 지킬 규칙: 알림 켜기·끄기는 PIN이 맞을 때만 저장 / GitHub Actions로 매일 한국시간(KST) 기준 오전 8시, 오후 8시에 자동 실행 / 그 시점 주요 기사 5건(AI 3건 + 반도체 2건, 규칙은 Must 1과 같음)을 한 번에 웹 푸시 알림으로 발송 (기사마다 알림 1건) / 최근 90일 안에 알린 기사와 겹치면 제외하고 다음 순위 기사로 대체 / 새 기사가 5건 미만이면 있는 만큼만 발송하고, 0건이면 발송을 생략 / 알림을 누르면 앱의 해당 기사 화면으로 이동 / 발송 실패 시 1회 재시도 후 실패 로그 기록 (단, 기기 정보가 만료된 경우에는 재시도하지 않고 기록한 뒤, 앱에서 알림을 다시 켜도록 안내)

**Nice to have**
- 한국어 요약과 쉬운 설명 — 실시간 화면, 기사 화면, 알림의 기사에 OpenAI API로 한국어 제목과 핵심 3줄 요약(무슨 일 → 자세한 내용 → 왜 중요한지)을 붙이고, 새로운 AI·반도체 기술이 나오면 쉬운 말로 한 줄 설명 추가. 한 번 만든 요약은 저장해 두고 다시 쓴다. (2026-09-18 사용자 요청으로 1~2줄 → 3줄 요약으로 변경)
- 요약 AI 모델 고르기 — 메인 화면 버튼으로 한국어 요약에 쓸 모델을 GPT-5.5(정확함·비용 높음)와 GPT-5.4 mini(빠름·비용 낮음) 중에서 고른다. 고른 모델은 저장소에 저장돼 화면과 알림 모두 새로 만드는 요약부터 적용되고, 이미 만든 요약은 그대로 둔다. 바꿀 때는 알림 켜기와 같은 PIN을 넣어야 한다 (목록에 없는 모델은 저장하지 않음). (2026-09-17 사용자 요청으로 용어 사전 링크 기능을 빼고 대신 추가, 2026-09-18 보안 점검 뒤 PIN 추가)
- 주요 뉴스 영상 1개 — 해외 뉴스 채널(CNBC, Bloomberg, Reuters, WSJ, Yahoo Finance)의 유튜브 RSS에서 최근 2일 안에 올라온 AI·반도체 영상 중 조회수가 가장 많은 1개를 메인 화면에서 바로 재생. 채널별 결과를 12시간 동안 저장해 12시간마다 교체. 가져오지 못하면 영상 칸에 "지금은 보여줄 영상이 없어요"를 작게 표시 (유튜브 RSS가 자주 실패해 영상이 빠지는 때가 있음)
- 지난 기사 목록 보기 — 최근 90일 동안 알림으로 보낸 기사를 최신순으로 한국어 제목, 매체, 보낸 날짜와 함께 보여주는 화면. 기사를 누르면 기사 화면에서 요약과 원문 링크를 볼 수 있다

## 6) 범위와 비범위

**이번에 만들 것**
- 실시간 인기 기사 검색·표시 화면
- 알림을 누르면 열리는 기사 화면
- PIN을 확인하는 알림 켜기·끄기
- 12시간마다 자동 실행되는 인기 기사 선정 + 웹 푸시 알림 발송
- 이미 알린 기사와 중복되지 않도록 확인하는 로직
- 알림 보낸 기사와 알림 받을 기기 정보 저장 — 비공개 GitHub 저장소 안의 파일에 저장 (GitHub Actions가 읽고 쓸 수 있게), 보낸 기사는 최근 90일치만 보관
- 휴대폰 홈 화면에 추가해 앱처럼 쓸 수 있는 웹앱 설정
- (여유가 있으면) 한국어 요약과 새 기술 쉬운 설명
- (여유가 있으면) 요약 AI 모델 고르기 버튼
- (여유가 있으면) 지난 기사 목록 화면
- (여유가 있으면) 메인 화면의 주요 뉴스 영상 1개 (12시간마다 교체)

**절대 만들지 않을 것**
- 앱스토어·플레이스토어에 올리는 네이티브 앱
- 텔레그램 등 다른 메신저로의 발송
- 여러 사용자 지원 (로그인, 계정 관리) — 본인 전용 단일 사용자로 한정
- 관심 키워드 커스터마이징
- 한국어 외 다른 언어로의 출력
- 오픈율 등 운영 지표를 재는 별도 측정 기능

## 7) 보안·개인정보 검토

- **공개등급**: 개인용 비공개 프로젝트. 본인만 사용
- **개인정보 여부**: 별도의 회원 정보는 수집하지 않으며, 알림을 받을 본인 기기의 푸시 구독 정보만 보관. 이 정보가 GitHub 저장소 파일에 들어가므로 저장소는 반드시 비공개로 유지
- **알림 설정 보호**: 로그인이 없으므로, 알림 켜기·끄기는 `.env`에 저장한 PIN이 맞을 때만 반영해 다른 사람이 알림 설정을 바꾸지 못하게 함
- **API 키 관리**: 웹 푸시용 키, 번역/요약용 OpenAI API 키, GitHub 토큰, PIN 등은 모두 `.env` 파일에 저장하고 코드에 직접 작성하지 않음. `.env`는 `.gitignore`에 이미 등록되어 있어 GitHub에 올라가지 않음. 배포 시에는 같은 값을 GitHub Actions Secrets와 Vercel 환경변수에 등록

## 8) 기술 스택

- **Next.js**: 화면과 서버 쪽 코드(API)를 한 프로젝트에서 처리
- **고정 이유**: STEP 2에서 `my-app`에 이미 설치했고, Part 5에서 Vercel로 배포할 예정이라 그대로 이어지게 하기 위함

## 개발 단위

이 순서대로 하나씩 만들어갈게요. (PLAN.md 작업 순서와 같음)

1. 기반 준비 — 비공개 저장소 만들기, 기사와 저장 파일 모양 정하기
2. 뉴스 가져오기 — 구글 뉴스에서 AI 기사와 반도체 기사를 따로 찾고, AI 3개 + 반도체 2개 고르기 (이미 보낸 기사는 빼기)
3. 화면 만들기 — 앱을 열면 주요 기사 5개가 보이는 메인 화면과, 알림을 누르면 열리는 기사 화면
4. 앱처럼 쓰기 — 휴대폰 홈 화면에 추가할 수 있게 설정하기
4-1. (Nice to have, 먼저 진행) 뉴스 영상 붙이기 — 해외 뉴스 채널 유튜브 영상 중 AI·반도체 영상 1개를 메인 화면에 넣기
5. 알림 켜기 — PIN을 확인하고 알림 받을 기기 정보 저장하기
6. 알림 보내기 — 아침 8시, 저녁 8시에 주요 기사 최대 5개를 알림으로 보내고, 보낸 기사를 저장하기
7. (Nice to have) 한국어로 바꾸기 — 한국어 제목·요약, 새 기술 쉬운 설명 붙이기, 요약 모델 고르기 버튼
8. (Nice to have) 지난 기사 화면 만들기 — 저장해둔 기사를 목록으로 보여주기
